# Same Supabase project as mason-web / masonadmin (copy from mason-web/.env)
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# Optional: override main site link when site_settings.main_site_url is unset or for local dev
# Production: https://sexwithmason.com (also configurable in masonadmin → Settings → Public URLs)
# VITE_MAIN_SITE_URL=http://localhost:5173
